  # Pool Outside Temperature
  - platform: dallas_temp
    address: !secret dallas_address_pool_outside_temp
    name: Outside Temperature
    id: ${device_internal_name}_outside_temperature

  # Pool Water Temperature
  - platform: dallas_temp
    address: !secret dallas_address_pool_water_temp
    name: Water Temperature
    id: ${device_internal_name}_water_temperature

